# Taste
- Communicates in Indonesian (Bahasa Indonesia). Confidence: 0.95
- Tracks multi-part fixes with an explicit task/todo list, and pauses to ask the user for clarification before guessing on decisions that require info only the user has (e.g., a real production CMS URL), while proceeding with the unambiguous parts. Confidence: 0.75
- Prefers ready-to-use, copy-paste AI prompts with complete theme specs (fonts, light/dark palettes, design tokens) as deliverables for aligning external systems (e.g., a Laravel CMS) with the portfolio theme, rather than direct code changes in those external systems. Confidence: 0.6
- Prefers thorough root-cause investigation using actual data/API responses (curl, reading CMS payloads) before proposing fixes, rather than jumping to code changes based on assumptions. Confidence: 0.8
- When a bug has both a data-side and a code-side cause, prefers to fix the data/root cause (env config, assets) rather than masking missing data in code. When offered multiple solutions, initially picks the fastest one that requires no code changes — but readily switches to a code-only fallback (e.g., "bikin fallback by slug aja") as soon as the env/data fix is blocked by missing external info (e.g., no production CMS URL exists), instead of waiting for that prerequisite. Confidence: 0.8
- When the agent proposes a root cause that doesn't match the actual symptom — or attributes a problem to an environment constraint (e.g., CORS in local preview) and the user later finds the symptom still persists on specific items (e.g., "image Petly dan JoyStickMania belum muncul") — the user reports the concrete residual symptom and expects the investigation to continue (digging into real data: CMS payloads, image URL status, file contents/hashes) until the true cause is found, rather than accepting a plausible-sounding explanation. Confidence: 0.8
- When a fix would require adding external asset files (e.g., downloading fallback images into `public/`), prefers a self-contained code solution that reuses already-defined fallback data (matched by slug) instead — "bikin fallback by slug aja" — keeping the change minimal and free of extra files. Confidence: 0.75
- Wants `.env`/`.env.example` files organized with clearly-labeled commented sections (e.g., `===== LOCAL / DEVELOPMENT =====` and `===== PRODUCTION =====`) showing both the active and commented-out variants, so it's obvious which config line to use in each environment. Confidence: 0.8
- Is performance-conscious about production load times on image-heavy pages (e.g., the Projects section) and explicitly asks for optimization guidance focused on the CMS/backend source (thumbnails, compression) rather than only client-side tweaks. Confidence: 0.5
- When offered a choice between implementing optimizations directly in the current project now or providing guidance/code for external systems, prefers the in-project implementation first ("kerjakan di projek ini dahulu") — frontend changes are applied directly and verified, while external backend (e.g., Laravel CMS) changes are deferred to later guidance, which the user then explicitly approves ("boleh"). Confidence: 0.8
- When handing over a design deliverable to integrate (e.g., an extracted "Redesign ..." folder/zip containing a standalone implementation of a page), expects the agent to inspect the folder first ("coba dicek") and then port it into the existing app while preserving its established systems (CMS data fetch, i18n keys/translations, dark-mode toggle, theme variables) rather than rebuilding the page from scratch or dropping those features; the source folder is left intact as reference. Confidence: 0.65
- For Figma-driven page redesigns, keeps the provided design's layout and aesthetic but expects real content underneath: skeleton/CSS-mockup placeholders are only interim, and the user will later ask to replace them with actual data and images from the database/CMS rendered responsively without stretch. Expects redesigned pages to keep following the app's existing systems (dark-mode toggle, theme variables, i18n). Confidence: 0.7
- Once a design/reference folder has been fully ported into the app and nothing references it anymore, proactively deletes the leftover folder from the project to keep the workspace clean — but first asks/expects confirmation that it is truly unused (agent verifies via code-reference search and git status before the destructive delete). Confidence: 0.6
- Avoids duplicating UI chrome that already exists globally: when a page design mockup includes a page-local navbar but the site already has a global Navbar, the duplicate is removed in favor of the existing global component ("navbar design dari figma hapus aja karena kan sudah ada"). Confidence: 0.85
- Wants page content to come genuinely from the app's database/CMS rather than hardcoded or newly-invented static content ("data2-nya ambil dari database dong"); any offline fallback should reuse the existing bilingual i18n keys already in the repo, not fabricated copy. Confidence: 0.85
- For displayed images (e.g., project screenshots), expects images of any source dimension to render responsively without stretching or distorting — aspect-ratio containers with object-fit so oversized/wide images letterbox instead of being cropped or stretched. Confidence: 0.9
- Runs a local development setup with its own local CMS/database whose content can differ from or lag production; when reporting missing content/images the user refers to that local instance and corrects analyses drawn from the production API ("eh ini local belum ada image nya") — confirm which environment the symptom occurs in and check that environment's actual data before blaming the code. Confidence: 0.6
- Treats mobile viewports as a first-class target: when the mobile layout hides/cuts off content (list and detail stacked, bottom clipped, action buttons unreachable), the user reports it with a screenshot and iterates until every section is reachable. Settled preferences after iteration: list/detail are mutually exclusive screens (list OR detail, not both stacked); on mobile the page should scroll naturally as a normal document — content-driven height rather than a forced full-viewport flex container, and no nested internal scroll areas that trap the scroll ("scroll-nya ga mau ke scroll halamannya"); interactive buttons (e.g., Prev/Next) must stay comfortably tappable — not pinned to the bottom edge by a stretched layout, not pushed out of reach, and not overlapped by fixed floating widgets (e.g., chat FAB); dvh-aware sizing where fixed chrome remains. Confidence: 0.8
- When the user says to remove a visual element that breaks the mobile layout (e.g., an oversized empty gallery/placeholder that dominates the screen), they mean removing the *empty placeholder* — not the real content: actual project screenshot images must remain visible on mobile. If the agent over-removes and hides the real image (e.g., `display: none` on the whole gallery), the user immediately reports the regression ("kok image nya ilang") and expects the image restored. The accepted middle ground: keep the main screenshot in a compact 16:9 frame with a modest max-height (~220px) and object-fit contain, hiding only auxiliary chrome such as the thumbnail strip. Confidence: 0.85
- When an element appears unclickable ("ga bisa di klik"), expects the agent to find the true cause by inspecting what actually covers it at hit-test time (agent-browser getBoundingClientRect on the element and on fixed overlays), not to assume a scroll/layout issue — e.g., the real culprit was a transparent-but-layout-occupying chat window stretching the fixed `.chatbot-root` across the bottom-right and swallowing clicks. Confidence: 0.8
- Closed/collapsed UI overlays (e.g., a chat widget whose window is hidden via opacity/pointer-events but still occupies the flex layout) must not keep claiming page space that blocks underlying content — hidden states should use absolute positioning / visibility / display so the collapsed widget shrinks to its FAB footprint. Confidence: 0.7
- Wants "anti-mainstream" (anti-mainstream = distinctive, unconventional) UI presentation: when replacing a plain media area, expects a striking custom effect — e.g., a 3D coverflow slider with the active image large in the center and neighbors shrunk, tilted (rotateY), and receding (translateZ) behind it — rather than a standard carousel or plain thumbnail strip. Confidence: 0.7
- Reapplies the "developer AI banget" card-slider language to each new image-bearing content type rather than leaving static grids: after project screenshots, certificates were also requested as a coverflow card slider ("sertifikat dibuat card slider dengan animasi developer AI banget") — terminal-style chrome, autoplay that pauses permanently on user interaction, and click-active-card-for-detail lightbox, with the effect centered at every viewport (desktop & mobile) so card center = stage center. Confidence: 0.6
- Prefers consolidating auxiliary per-item UI out of detail views (e.g., a screenshot "pages" progress bar/card and the bottom Prev/Next project footer were removed from the Projects detail): content should flow directly, and image browsing is handled by one self-contained slider (arrows, counter, caption, swipe, click-neighbor-to-jump) instead of scattered chrome blocks around/below the content. Confidence: 0.6
- Validates delivered visual effects in real conditions — mobile viewports and realistic content volumes (e.g., 9 slider images at index 04) — and reports pixel-level misalignment tersely with a screenshot ("pas muncul ga center"); geometry that was verified only on desktop with few items is expected to hold centered on mobile with many items too, so responsive CSS tweaks must not diverge from the JS positioning math that the effect depends on. Confidence: 0.6
- Values being able to inspect project screenshots at full detail even when the page presents them in a compact, stylized preview (coverflow): prefers keeping the preview aesthetic and adding a click-to-open full-size lightbox (dark overlay, prev/next arrows, counter, caption, ESC/backdrop close, keyboard arrows, auto-close on project switch) rather than permanently enlarging the card/slider itself at the cost of the layout. Confidence: 0.65
- When unsure which UX approach fits (e.g., slider images too small to read details), asks the agent for a recommendation ("saran kamu gimana... apakah ... atau gimana") and expects a concrete standard best-practice proposal — then follows the agent's recommended option (lightbox) once presented, instead of the agent unilaterally committing to a big layout change. Confidence: 0.55
- Wants hidden/undiscoverable interactions to carry an explicit visible text cue — asked to add "teks keterangan untuk klik gambar untuk melihat detail" (a click-to-zoom affordance label under the slider) rather than relying on hover-only icons that are invisible on mobile; the hint should itself be tappable/clickable and follow the site's i18n (bilingual en/id keys), not hardcoded text. Confidence: 0.7
- Extends the "content from the database/CMS, not hardcoded" principle to AI features: expects the chatbot's knowledge to be driven dynamically by the site's CMS content ("berdasarkan hasil inputan dari CMS") so the bot understands questions about the current portfolio info and stays up to date with CMS edits, rather than answering from a static/hardcoded prompt. Confidence: 0.7
- Cares that third-party API keys/secrets never reach the browser bundle — when choosing chatbot architecture, deliberately picked the server-side proxy option (Groq call moved behind a Vercel Function, key in a server-only env var) over the client-side integration, explicitly citing "keamanan jangka panjang" (long-term security). Confidence: 0.75
- When offered tiered options for an AI feature (quick dynamic prompt-stuffing vs. full server-side proxy + retrieval/RAG), chooses the most robust, more-work option rather than the fastest fix — wants the bot to reliably understand questions and answer strictly per the current website/CMS content (grounded, no stale/hallucinated answers), treating correctness and security as worth the extra architecture even after the quick fix was recommended first. Confidence: 0.7
- When the agent mentions a future/optional enhancement (e.g., embeddings + vector search documented as a next step "when content grows"), the user later asks point-blank whether it was actually implemented ("embeddings + vector search sudah kamu implementasikan?") — expecting an honest, precise status that clearly separates what is live in the code from what is only documented/planned, rather than any overstatement of progress. Confidence: 0.5
- Is cost-conscious about third-party AI/API services and probes for free options before adding providers: runs on free-tier accounts (Groq) and, when told a paid provider (OpenAI embeddings) has no lasting free tier, immediately asks whether alternatives exist ("apakah makai openAI key yang gratisan?") — expects the agent to verify current free-tier availability of other providers (e.g., Gemini AI Studio no-credit-card limits) rather than assume. Confidence: 0.55
- When an architecture or approach is proposed or contrasted (e.g., current full-context chatbot vs. embeddings + vector search/RAG), asks conceptual "apa bedanya?" questions to genuinely understand the trade-offs before deciding anything — expects plain-language explanations with analogies and a practical comparison (accuracy at small vs. large data scale, cost, complexity, update behavior), plus an honest recommendation that the simpler existing approach may already be superior, rather than jargon-only or a terse "you don't need it." Confidence: 0.45
- When AI chatbot answers contain markdown formatting (bold `**`, GFM tables, `<br>`), expects the chat UI to render it as rich formatted text — seeing literal markdown syntax like `**` in the output is reported as a visual defect ("text bold ga ke detect ... kurang tampilannya"), not acceptable as-is. Confidence: 0.6
- Wants AI chat features to look convincingly "AI" when asking for a redesign ("revamp desain chat bot nya jadi lebih AI banget"): modern AI-suite styling — gradient accent header with decorative glow/orb, glassmorphism, sparkle iconography, an "AI Assistant" chip, typing indicator, quick-action suggestion chips — kept on-theme with the site's existing CSS variables rather than a plain chat panel. Confidence: 0.55
- Chat/conversation widgets must keep the newest message in view as the thread grows — long chats that strand the view at the top while new replies arrive out of sight ("kalau chat nya kepanjangan dia sampe atas gitu") are reported as a defect; expects reliable container-level auto-scroll to the latest message, plus a visible floating "jump to latest" button once the user scrolls up through history. Confidence: 0.6
- Prefers the tech-stack/skills section rendered as a continuous animated horizontal marquee ("animasi scroll kesamping") instead of static cards: two rows scrolling in opposite directions with a seamless loop (track duplicated and translated −50%), masked edge fade on left/right, icon-in-each-pill, and per-pill hover effects (lift + scale, accent border/text, glow) that pause the scroll so items stay readable. Confidence: 0.6
- Expects decorative/entrance animations to respect `prefers-reduced-motion`: animated grid/tokens/orbs are hidden or stopped and typed text appears instantly for users who opt out of motion, while functional content (scroll-reveal fallback, marquee pausing on hover) still works. Confidence: 0.6
- Extends the "developer AI" (AI banget) aesthetic beyond the chat widget to the whole Home page: when offered concrete visual directions for a page revamp, picks the richer combined route (terminal/IDE-style hero — typewriter role text with `# Taste
- Communicates in Indonesian (Bahasa Indonesia). Confidence: 0.95
- Tracks multi-part fixes with an explicit task/todo list, and pauses to ask the user for clarification before guessing on decisions that require info only the user has (e.g., a real production CMS URL), while proceeding with the unambiguous parts. Confidence: 0.75
- Prefers ready-to-use, copy-paste AI prompts with complete theme specs (fonts, light/dark palettes, design tokens) as deliverables for aligning external systems (e.g., a Laravel CMS) with the portfolio theme, rather than direct code changes in those external systems. Confidence: 0.6
- Prefers thorough root-cause investigation using actual data/API responses (curl, reading CMS payloads) before proposing fixes, rather than jumping to code changes based on assumptions. Confidence: 0.8
- When a bug has both a data-side and a code-side cause, prefers to fix the data/root cause (env config, assets) rather than masking missing data in code. When offered multiple solutions, initially picks the fastest one that requires no code changes — but readily switches to a code-only fallback (e.g., "bikin fallback by slug aja") as soon as the env/data fix is blocked by missing external info (e.g., no production CMS URL exists), instead of waiting for that prerequisite. Confidence: 0.8
- When the agent proposes a root cause that doesn't match the actual symptom — or attributes a problem to an environment constraint (e.g., CORS in local preview) and the user later finds the symptom still persists on specific items (e.g., "image Petly dan JoyStickMania belum muncul") — the user reports the concrete residual symptom and expects the investigation to continue (digging into real data: CMS payloads, image URL status, file contents/hashes) until the true cause is found, rather than accepting a plausible-sounding explanation. Confidence: 0.8
- When a fix would require adding external asset files (e.g., downloading fallback images into `public/`), prefers a self-contained code solution that reuses already-defined fallback data (matched by slug) instead — "bikin fallback by slug aja" — keeping the change minimal and free of extra files. Confidence: 0.75
- Wants `.env`/`.env.example` files organized with clearly-labeled commented sections (e.g., `===== LOCAL / DEVELOPMENT =====` and `===== PRODUCTION =====`) showing both the active and commented-out variants, so it's obvious which config line to use in each environment. Confidence: 0.8
- Is performance-conscious about production load times on image-heavy pages (e.g., the Projects section) and explicitly asks for optimization guidance focused on the CMS/backend source (thumbnails, compression) rather than only client-side tweaks. Confidence: 0.5
- When offered a choice between implementing optimizations directly in the current project now or providing guidance/code for external systems, prefers the in-project implementation first ("kerjakan di projek ini dahulu") — frontend changes are applied directly and verified, while external backend (e.g., Laravel CMS) changes are deferred to later guidance, which the user then explicitly approves ("boleh"). Confidence: 0.8
- When handing over a design deliverable to integrate (e.g., an extracted "Redesign ..." folder/zip containing a standalone implementation of a page), expects the agent to inspect the folder first ("coba dicek") and then port it into the existing app while preserving its established systems (CMS data fetch, i18n keys/translations, dark-mode toggle, theme variables) rather than rebuilding the page from scratch or dropping those features; the source folder is left intact as reference. Confidence: 0.65
- For Figma-driven page redesigns, keeps the provided design's layout and aesthetic but expects real content underneath: skeleton/CSS-mockup placeholders are only interim, and the user will later ask to replace them with actual data and images from the database/CMS rendered responsively without stretch. Expects redesigned pages to keep following the app's existing systems (dark-mode toggle, theme variables, i18n). Confidence: 0.7
- Once a design/reference folder has been fully ported into the app and nothing references it anymore, proactively deletes the leftover folder from the project to keep the workspace clean — but first asks/expects confirmation that it is truly unused (agent verifies via code-reference search and git status before the destructive delete). Confidence: 0.6
- Avoids duplicating UI chrome that already exists globally: when a page design mockup includes a page-local navbar but the site already has a global Navbar, the duplicate is removed in favor of the existing global component ("navbar design dari figma hapus aja karena kan sudah ada"). Confidence: 0.85
- Wants page content to come genuinely from the app's database/CMS rather than hardcoded or newly-invented static content ("data2-nya ambil dari database dong"); any offline fallback should reuse the existing bilingual i18n keys already in the repo, not fabricated copy. Confidence: 0.85
- For displayed images (e.g., project screenshots), expects images of any source dimension to render responsively without stretching or distorting — aspect-ratio containers with object-fit so oversized/wide images letterbox instead of being cropped or stretched. Confidence: 0.9
- Runs a local development setup with its own local CMS/database whose content can differ from or lag production; when reporting missing content/images the user refers to that local instance and corrects analyses drawn from the production API ("eh ini local belum ada image nya") — confirm which environment the symptom occurs in and check that environment's actual data before blaming the code. Confidence: 0.6
- Treats mobile viewports as a first-class target: when the mobile layout hides/cuts off content (list and detail stacked, bottom clipped, action buttons unreachable), the user reports it with a screenshot and iterates until every section is reachable. Settled preferences after iteration: list/detail are mutually exclusive screens (list OR detail, not both stacked); on mobile the page should scroll naturally as a normal document — content-driven height rather than a forced full-viewport flex container, and no nested internal scroll areas that trap the scroll ("scroll-nya ga mau ke scroll halamannya"); interactive buttons (e.g., Prev/Next) must stay comfortably tappable — not pinned to the bottom edge by a stretched layout, not pushed out of reach, and not overlapped by fixed floating widgets (e.g., chat FAB); dvh-aware sizing where fixed chrome remains. Confidence: 0.8
- When the user says to remove a visual element that breaks the mobile layout (e.g., an oversized empty gallery/placeholder that dominates the screen), they mean removing the *empty placeholder* — not the real content: actual project screenshot images must remain visible on mobile. If the agent over-removes and hides the real image (e.g., `display: none` on the whole gallery), the user immediately reports the regression ("kok image nya ilang") and expects the image restored. The accepted middle ground: keep the main screenshot in a compact 16:9 frame with a modest max-height (~220px) and object-fit contain, hiding only auxiliary chrome such as the thumbnail strip. Confidence: 0.85
- When an element appears unclickable ("ga bisa di klik"), expects the agent to find the true cause by inspecting what actually covers it at hit-test time (agent-browser getBoundingClientRect on the element and on fixed overlays), not to assume a scroll/layout issue — e.g., the real culprit was a transparent-but-layout-occupying chat window stretching the fixed `.chatbot-root` across the bottom-right and swallowing clicks. Confidence: 0.8
- Closed/collapsed UI overlays (e.g., a chat widget whose window is hidden via opacity/pointer-events but still occupies the flex layout) must not keep claiming page space that blocks underlying content — hidden states should use absolute positioning / visibility / display so the collapsed widget shrinks to its FAB footprint. Confidence: 0.7
- Wants "anti-mainstream" (anti-mainstream = distinctive, unconventional) UI presentation: when replacing a plain media area, expects a striking custom effect — e.g., a 3D coverflow slider with the active image large in the center and neighbors shrunk, tilted (rotateY), and receding (translateZ) behind it — rather than a standard carousel or plain thumbnail strip. Confidence: 0.7
- Prefers consolidating auxiliary per-item UI out of detail views (e.g., a screenshot "pages" progress bar/card and the bottom Prev/Next project footer were removed from the Projects detail): content should flow directly, and image browsing is handled by one self-contained slider (arrows, counter, caption, swipe, click-neighbor-to-jump) instead of scattered chrome blocks around/below the content. Confidence: 0.6
- Validates delivered visual effects in real conditions — mobile viewports and realistic content volumes (e.g., 9 slider images at index 04) — and reports pixel-level misalignment tersely with a screenshot ("pas muncul ga center"); geometry that was verified only on desktop with few items is expected to hold centered on mobile with many items too, so responsive CSS tweaks must not diverge from the JS positioning math that the effect depends on. Confidence: 0.6
- Values being able to inspect project screenshots at full detail even when the page presents them in a compact, stylized preview (coverflow): prefers keeping the preview aesthetic and adding a click-to-open full-size lightbox (dark overlay, prev/next arrows, counter, caption, ESC/backdrop close, keyboard arrows, auto-close on project switch) rather than permanently enlarging the card/slider itself at the cost of the layout. Confidence: 0.65
- When unsure which UX approach fits (e.g., slider images too small to read details), asks the agent for a recommendation ("saran kamu gimana... apakah ... atau gimana") and expects a concrete standard best-practice proposal — then follows the agent's recommended option (lightbox) once presented, instead of the agent unilaterally committing to a big layout change. Confidence: 0.55
- Wants hidden/undiscoverable interactions to carry an explicit visible text cue — asked to add "teks keterangan untuk klik gambar untuk melihat detail" (a click-to-zoom affordance label under the slider) rather than relying on hover-only icons that are invisible on mobile; the hint should itself be tappable/clickable and follow the site's i18n (bilingual en/id keys), not hardcoded text. Confidence: 0.7
- Extends the "content from the database/CMS, not hardcoded" principle to AI features: expects the chatbot's knowledge to be driven dynamically by the site's CMS content ("berdasarkan hasil inputan dari CMS") so the bot understands questions about the current portfolio info and stays up to date with CMS edits, rather than answering from a static/hardcoded prompt. Confidence: 0.7
- Cares that third-party API keys/secrets never reach the browser bundle — when choosing chatbot architecture, deliberately picked the server-side proxy option (Groq call moved behind a Vercel Function, key in a server-only env var) over the client-side integration, explicitly citing "keamanan jangka panjang" (long-term security). Confidence: 0.75
- When offered tiered options for an AI feature (quick dynamic prompt-stuffing vs. full server-side proxy + retrieval/RAG), chooses the most robust, more-work option rather than the fastest fix — wants the bot to reliably understand questions and answer strictly per the current website/CMS content (grounded, no stale/hallucinated answers), treating correctness and security as worth the extra architecture even after the quick fix was recommended first. Confidence: 0.7
- When the agent mentions a future/optional enhancement (e.g., embeddings + vector search documented as a next step "when content grows"), the user later asks point-blank whether it was actually implemented ("embeddings + vector search sudah kamu implementasikan?") — expecting an honest, precise status that clearly separates what is live in the code from what is only documented/planned, rather than any overstatement of progress. Confidence: 0.5
- Is cost-conscious about third-party AI/API services and probes for free options before adding providers: runs on free-tier accounts (Groq) and, when told a paid provider (OpenAI embeddings) has no lasting free tier, immediately asks whether alternatives exist ("apakah makai openAI key yang gratisan?") — expects the agent to verify current free-tier availability of other providers (e.g., Gemini AI Studio no-credit-card limits) rather than assume. Confidence: 0.55
- When an architecture or approach is proposed or contrasted (e.g., current full-context chatbot vs. embeddings + vector search/RAG), asks conceptual "apa bedanya?" questions to genuinely understand the trade-offs before deciding anything — expects plain-language explanations with analogies and a practical comparison (accuracy at small vs. large data scale, cost, complexity, update behavior), plus an honest recommendation that the simpler existing approach may already be superior, rather than jargon-only or a terse "you don't need it." Confidence: 0.45
- When AI chatbot answers contain markdown formatting (bold `**`, GFM tables, `<br>`), expects the chat UI to render it as rich formatted text — seeing literal markdown syntax like `**` in the output is reported as a visual defect ("text bold ga ke detect ... kurang tampilannya"), not acceptable as-is. Confidence: 0.6
- Wants AI chat features to look convincingly "AI" when asking for a redesign ("revamp desain chat bot nya jadi lebih AI banget"): modern AI-suite styling — gradient accent header with decorative glow/orb, glassmorphism, sparkle iconography, an "AI Assistant" chip, typing indicator, quick-action suggestion chips — kept on-theme with the site's existing CSS variables rather than a plain chat panel. Confidence: 0.55
- Chat/conversation widgets must keep the newest message in view as the thread grows — long chats that strand the view at the top while new replies arrive out of sight ("kalau chat nya kepanjangan dia sampe atas gitu") are reported as a defect; expects reliable container-level auto-scroll to the latest message, plus a visible floating "jump to latest" button once the user scrolls up through history. Confidence: 0.6
 prompt + blinking caret, an IDE-style config/code card with traffic-light dots whose lines appear one by one, floating code tokens, animated grid + drifting glow orbs — plus consistent staggered scroll-triggered reveal across every section) rather than a minimal or purely decorative treatment. Confidence: 0.55
- Expects a chosen page style/design language to be propagated site-wide: after the Home page got the "developer AI" treatment, the user instructs "terapkan gaya di home ke project, education, experience, dan contact" — wanting the same shared visual system (grid/glow background, scroll-reveal, terminal accents) applied consistently across all remaining content pages rather than leaving them in the old look, with the agent expected to extract shared components/CSS (AiBackground, Reveal, `.rv`/`.ai-bg`) for reuse instead of duplicating per page. Confidence: 0.55
